services:
  server:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - DB_PATH=/app/data/termenor.db
    volumes:
      - termenor-data:/app/data
    restart: unless-stopped

volumes:
  termenor-data:
